import { css } from "@emotion/react";

const SideBar = () => {
    return <div css={styles.wrapper}>aaaa</div>;
};

export default SideBar;

const styles = {
    wrapper: css`
        border-left: 1px solid #edebe4;
        width: 100%;
        height: 100%;
    `,
};
